---
title: The title of the entry
date: 2026-07-11
dek: One line under the title. What the reader gets out of this.
cover: /journal/the-slug.png
tags: [building, learning]
---

<!-- cover is optional. Drop the image in public/journal/ and point at it with a
     leading slash. Leave it out entirely and the card falls back to setting the
     title in type — publish first, make the artwork later. Keep images under
     ~300kb; a 4MB screenshot will tank the page. -->


Copy this file, rename it to a slug — `what-i-learned-shipping-slapshift.md` —
and write. The filename becomes the URL: `/journal/what-i-learned-shipping-slapshift`.

Write in markdown. **Bold**, *italic*, `code`, and lists all work.

## Subheads work too

> Pull quotes work. Use them when a line is worth stealing.

Delete this file's contents when you copy it — the leading underscore keeps
`_template.md` itself off the site.
